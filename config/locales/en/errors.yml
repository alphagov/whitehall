en:
  errors:
    messages:
      blank: "cannot be blank"
      extension_allowlist_error: "is of not allowed type %{extension}, allowed types: %{allowed_types}"
      extension_denylist_error: "is of not allowed type %{extension}, prohibited types: %{prohibited_types}"
  activerecord:
    attributes:
      publication/nation_inapplicabilities:
        alternative_url: "Alternative URL for excluded nation"
    errors:
      messages:
        invalid_date: "is not in the correct format"
      models:
        edition:
          attributes:
            first_published_at:
              format: "%{message}"
              invalid_date: First published date is not in the correct format
              blank: Enter a first published date
              inclusion: First published date must be between 1/1/1900 and the present
            html_attachments:
              missing_contact_reference: "- \"%{html_attachment_title}\" - embedded Contact (ID %{contact_id}) doesn't exist"
            unpublishing:
              invalid: "has an invalid redirect URL or reason"
        nation_inapplicability:
          attributes:
            alternative_url:
              too_long: "is too long. Please shorten the URL to less than 255 characters."
