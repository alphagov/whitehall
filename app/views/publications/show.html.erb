<div class="g2">
  <%= render partial: 'document', locals: { document: @document } %>
</div>

<div class="g1 secondary">
  <%= render partial: 'organisations', locals: { document: @document } %>

  <%= render partial: 'ministerial_roles', locals: { document: @document } %>

  <%= render partial: 'inapplicable_nations', locals: { document: @document } %>

  <% if @document.documents_related_to.published.any? %>
    <section id="related-documents" class="g1">
      <h1>Related policies</h1>
      <ul>
        <% @document.documents_related_to.published.each do |related_document| %>
          <%= content_tag_for :li, related_document do %>
            <%= link_to related_document.title, public_document_path(related_document) %>
          <% end %>
        <% end %>
      </ul>
    </section>
  <% end %>
</div>