<div class="format-advice">
  <p><strong>Use this format for:</strong> A plain English definition of an outcome government is working to achieve in a specific world location. Related content (news etc) should be associated to worldwide priorities.</p>
</div>

<%= edition_editing_tabs(edition) do %>
  <%= standard_edition_form(edition) do |form| %>

    <fieldset>
      <legend>Associations</legend>
      <%= render partial: 'specialist_sector_fields', locals: { form: form, edition: edition } %>
      <%= render partial: 'world_location_fields', locals: { form: form, edition: edition } %>
      <%= render partial: 'worldwide_organisation_fields', locals: { form: form, edition: edition } %>
      <%= render partial: 'organisation_fields', locals: { form: form, edition: edition } %>
    </fieldset>
  <% end %>
<% end %>
