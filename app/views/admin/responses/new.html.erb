<% page_title "Add response to: #{@edition.title}" %>

<div class="span8">
  <section>
    <h1>Response for: <%= @edition.title %></h1>

    <%= render partial: 'form', locals: { consultation: @edition, response: @response } %>
  </section>
</div>
