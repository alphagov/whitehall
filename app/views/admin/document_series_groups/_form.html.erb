<%= form_for [:admin, @series, @group], url: url do |form| %>
  <%= form.errors %>
  <%= form.text_field :heading, required: true %>
  <%= form.text_area :body, rows: 6, class: 'previewable' %>
  <%= form.save_or_cancel(cancel: admin_document_series_groups_path(@series)) %>
<% end %>
