<% page_title "Edit '#{@corporate_information_page.title}' for " + @organisation.name %>

<h1>Edit '<%= @corporate_information_page.title %>' page for <%= @organisation.name %></h1>

<div class="row-fluid">
  <div class="span8">
    <section>
      <%= render partial: "form", locals: {organisation: @organisation, corporate_information_page: @corporate_information_page} %>
    </section>
  </div>

  <div class="span4">
    <%= simple_formatting_sidebar %>
  </div>
</div>