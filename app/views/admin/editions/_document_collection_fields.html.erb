<%= form.label :document_collection_ids, 'Document collections' %>
<%= form.select :document_collection_ids, document_collection_select_options(edition, current_user), {}, multiple: true, class: 'chzn-select', data: { placeholder: "Choose collections this document is a part of..." } %>
