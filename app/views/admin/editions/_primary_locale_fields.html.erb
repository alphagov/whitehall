<% if edition.primary_locale_can_be_changed? %>
  <%= form.label :primary_locale %>
  <%= form.select :primary_locale, options_for_select(options_for_locales(Locale.all), edition.primary_locale) %>
<% end %>
