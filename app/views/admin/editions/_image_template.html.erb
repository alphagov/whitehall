<% edition.images.each_with_index do |image, index| %>
  <%= render Admin::Editions::ImageComponent.new(image: image, index: index, checked: params.dig("edition", "images_attributes", index.to_s, "_destroy") != "1") %>
<% end %>
