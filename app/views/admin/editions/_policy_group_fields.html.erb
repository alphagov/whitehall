<%= form.label :policy_group_ids, 'Groups' %>
<%= form.select :policy_group_ids, PolicyGroup.order(:name).all.map { |group|
  ["#{group.name} (#{group.email})", group.id]}, { include_blank: true },
  multiple: true,
  class: 'chzn-select',
  data: { placeholder: "Choose groups..."} %>
