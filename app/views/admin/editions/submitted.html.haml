=render partial: 'index_heading'

%section.resource_list
  %h1 Submitted
  %table#submitted_policies
    %thead
      %tr
        %th Title
        %th Author
        %th Type
        %th.actions Actions
    %tbody
    - @editions.each do |edition|
      = content_tag_for(:tr, edition, :class => 'policy') do
        %td.title= link_to edition.title, admin_edition_path(edition), title: "Show #{edition.title}"
        %td.author= edition.author.name
        %td.type= edition.document.type
        %td.actions
          = link_to 'Edit', edit_admin_edition_path(edition), title: "Edit #{edition.title}"
