<fieldset>
  <%= form.label :related_document_ids, 'Related policies' %>
  <%= form.select :related_document_ids, options_from_collection_for_select(related_policy_options, :first, :last, edition.related_document_ids), {}, multiple: true, class: 'chzn-select', data: { placeholder: "Choose related policies..."} %>
</fieldset>
