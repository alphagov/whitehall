<% page_title "Unpublish: #{@edition.title}"  %>

  <div class="span8">
    <section>
      <h1><%= @edition.title %></h1>

      <%= form_for @unpublishing, url: unpublish_admin_edition_path(@edition, lock_version: @edition.lock_version) do |form| %>

          <%= form.label :unpublishing_reason_id, "Do you want to archive or unpublish this document?" %>

          <%= form.radio_button :unpublishing_reason_id, UnpublishingReason::Archived.id %>
          <%= form.label "unpublishing_reason_id_#{UnpublishingReason::Archived.id}", "<strong>Archive:</strong> no longer current government policy/activity".html_safe %>

          <%= form.radio_button :unpublishing_reason_id, UnpublishingReason::PublishedInError.id %>
          <%= form.label "unpublishing_reason_id_#{UnpublishingReason::PublishedInError.id}", "<strong>Unpublish:</strong> published in error".html_safe %>

          <%= form.radio_button :unpublishing_reason_id, UnpublishingReason::Consolidated.id %>
          <%= form.label "unpublishing_reason_id_#{UnpublishingReason::Consolidated.id}", "<strong>Unpublish:</strong> consolidated into another GOV.UK page".html_safe %>

          <%= form.text_field :alternative_url, label_text: 'Alternative URL' %>
          <%= form.check_box :redirect, label_text: 'Redirect to URL automatically?' %>

          <%= form.text_area :explanation, label_text: '<strong>Public explanation</strong> (this is shown on the live site)'.html_safe, rows: 5, class: "previewable"  %>

          <%= form.submit 'Unpublish', class: "btn btn-danger" %>
          <span class="or_cancel"> or <%= link_to 'cancel', [:admin, @edition] %></span>
        <% end %>
    </section>
  </div>
