<%= form.label :world_location_ids, "Select the international locations this #{edition.format_name.titleize.downcase} is about" %>
<%= form.select :world_location_ids,
                options_from_collection_for_select(WorldLocation.all, 'id', 'name', edition.world_location_ids),
                { include_blank: true },
                multiple: true,
                class: 'chzn-select',
                data: { placeholder: "International locations..."} %>
