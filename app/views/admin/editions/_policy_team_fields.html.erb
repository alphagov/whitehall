<%= form.label :policy_team_ids, "Teams" %>
<%= form.select :policy_team_ids, PolicyTeam.order(:name).all.map { |team|
  ["#{team.name} (#{team.email})", team.id]}, { include_blank: true },
  multiple: true,
  class: 'chzn-select',
  data: { placeholder: "Choose teams..."} %>
