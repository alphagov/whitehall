<% cache_if edition.topical_event_ids.empty?, taggable_topical_events_cache_digest do %>
  <%= form.label :topical_event_ids, 'Topical events' %>
  <%= form.select :topical_event_ids,
                  options_from_collection_for_select(TopicalEvent.order(:name), 'id', 'name', edition.topical_event_ids),
                  {},
                  multiple: true,
                  class: 'chzn-select form-control',
                  data: {
                      placeholder: "Choose topical events…",
                      module: 'track-select-click',
                      track_category: 'topicalEventSelection',
                      track_label: request.path
                  } %>
<% end %>
