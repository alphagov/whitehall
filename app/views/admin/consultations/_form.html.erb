<%= standard_edition_form(edition) do |form| %>
  <%= render partial: 'attachment_fields', locals: { form: form, edition: edition, parent_type: :edition }%>

  <fieldset>
    <%= form.label :opening_on, 'Opening Date' %>
    <%= form.date_select :opening_on, { include_blank: true }, { :class => "date" } %>
    <%= form.label :closing_on, 'Closing Date' %>
    <%= form.date_select :closing_on, { include_blank: true }, { :class => "date" } %>
  </fieldset>

  <fieldset>
    <legend>Participation</legend>
    <%= form.text_field :consultation_participation_link_url, label_text: 'Participation link URL' %>
    <%= form.text_field :consultation_participation_link_text, label_text: 'Participation link text' %>
  </fieldset>

  <fieldset>
    <legend>Associations</legend>
    <%= render partial: 'related_policy_fields', locals: { form: form, edition: edition } %>
    <%= render partial: 'organisation_fields', locals: { form: form, edition: edition } %>
    <%= render partial: 'minister_fields', locals: { form: form, edition: edition } %>
    <%= render partial: 'nation_fields', locals: { form: form, edition: edition } %>
  </fieldset>
<% end %>
