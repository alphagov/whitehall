<fieldset>
  <%= form.label :related_policy_ids, 'Related Policies' %>
  <%= form.select :related_policy_ids, options_from_collection_for_select(Policy.all, 'id', 'title_with_state', document.related_policy_ids), {}, multiple: true, class: 'chzn-select', data: { placeholder: "Choose related policies..."} %>
</fieldset>
