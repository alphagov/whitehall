<% page_title "New " + @document.type.titleize %>
<div class="g2 first">
  <section>
    <h1>Create <%= @document.type.titleize %></h1>
    <%= render partial: "form", locals: {document: @document} %>
    <%= render partial: "govspeak_help" %>
  </section>
</div>