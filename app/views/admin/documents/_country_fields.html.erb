<fieldset>
  <%= form.label :country_ids, 'Countries' %>
  <%= form.select :country_ids, options_from_collection_for_select(Country.all, 'id', 'name', document.country_ids), {}, multiple: true, class: 'chzn-select', data: { placeholder: "Choose countries..."} %>
</fieldset>
