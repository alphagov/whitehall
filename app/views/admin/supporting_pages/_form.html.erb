<%= form_for [:admin, @document, supporting_page] do |form| %>
  <%= form.hidden_field :lock_version %>
  <%= form.text_field :title, disabled: disabled %>
  <%= form.text_area :body, disabled: disabled, "class" => "previewable", :help => true %>
  <% unless disabled %>
    <%= form.save_or_cancel cancel: (supporting_page.new_record? ? admin_document_path(@document) : admin_supporting_page_path(supporting_page))%>
  <% end %>
<% end %>