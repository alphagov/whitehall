<div class="govuk-grid-row">
	<div class="govuk-grid-column-two-thirds">
		<% content_for :page_title, "New office details" %>
		<% content_for :title, "New office details" %>
		<% content_for :context, @worldwide_organisation.name %>
		<% content_for :error_summary, render(Admin::ErrorSummaryComponent.new(object: @worldwide_office, parent_class: "worldwide_office")) %>

		<%= render partial: "form", locals: {worldwide_office: @worldwide_office, worldwide_organisation: @worldwide_organisation} %>
	</div>

	<div class="govuk-grid-column-one-third">
		<%= simple_formatting_sidebar %>
	</div>
</div>
