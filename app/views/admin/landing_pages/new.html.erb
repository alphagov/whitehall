<% content_for :page_title, "New landing page" %>
<% content_for :title, "New landing page" %>
<% content_for :error_summary, render(Admin::ErrorSummaryComponent.new(object: @landing_page)) %>

<%= render "form", landing_page: @landing_page %>
