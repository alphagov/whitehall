<% content_for :page_title, "New promotional feature for #{@organisation.name}" %>
<% content_for :title, "New promotional feature" %>
<% content_for :context, @organisation.name %>
<% content_for :error_summary, render(Admin::ErrorSummaryComponent.new(object: @promotional_feature)) %>


<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= render "govuk_publishing_components/components/warning_text", {
      text: "Newly created feature blocks will appear instantly on the live site."
    } %>

    <%= render "form", promotional_feature: @promotional_feature, organisation: @organisation %>
  </div>
</div>
