%h1 Government Department
%p
  So far you can view published policies, and if you
  =link_to "login", login_path
  you can write your own.
%section.resource_list
  %h1 Published policies
  %table#published_policies
    %thead
      %tr
        %th Title
        %th Author
        %th Type
    %tbody
      - @editions.each do |edition|
        = content_tag_for(:tr, edition, class: edition.document.class.name.downcase) do
          %td.title= link_to edition.title, policy_path(edition.document), title: "View #{edition.title}"
          %td.author= edition.author.name
          %td.type= edition.document.type
      - if @editions.empty?
        %tr
          %td{colspan: 2} Our authors have not published any policies at present.