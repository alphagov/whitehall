%h1 Policies

%p=link_to "Draft new policy", new_policy_path

%section.resource_list
  %h1 Drafts
  %ul#draft_policies
    - @policies.each do |policy|
      %li.policy.group
        %h2= policy.title
        .actions
          = link_to 'Edit', edit_policy_path(policy), :title => "Edit #{policy.title}"
