<ul class="organisations">
  <% organisations.each do |organisation| %>
    <%= content_tag_for(:li, organisation, class: "g1") do %>
      <%= if organisation.active?
            link_to organisation.name, organisation_path(organisation)
          else
            organisation.name
          end %>
    <% end %>
  <% end %>
</ul>
