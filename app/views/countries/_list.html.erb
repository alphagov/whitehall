<ul class="countries">
  <% countries.each do |country| %>
    <%= content_tag_for(:li, country, class: "g1") do %>
      <%= if country.active?
            link_to country.name, country_path(country)
          else
            country.name
          end %>
    <% end %>
  <% end %>
</ul>
