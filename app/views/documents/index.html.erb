<div class="g1"><%= render partial: "policies", object: @policies %></div>
<div class="g1"><%= render partial: "publications", object: @publications %></div>
<div class="g1"><%= render partial: "news_articles", object: @news_articles %></div>
<div class="g1"><%= render partial: "consultations", object: @consultations %></div>
<div class="g1"><%= render partial: "speeches", object: @speeches %></div>

<% if @policies.empty? && @publications.empty? && @news_articles.empty? && @consultations.empty? && @speeches.empty? %>
  <h1>Documents</h1>
  <p>We don't have any published documents at present.</p>
<% end %>