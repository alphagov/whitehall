<% if policies.any? %>
<section id="policies" class="g1">
  <h1>Policies</h1>
  <ul class="policies">
    <% policies.each do |policy| %>
      <%= content_tag_for(:li, policy) do %>
        <%= link_to policy.published_edition.title, document_path(policy), title: "View #{policy.published_edition.title}" %>
      <% end %>
    <% end %>
  </ul>
</section>
<% end %>
