<% page_title "Speeches" %>
<div class="g3">
  <% if @speeches.empty? %>
    <h1 class="page_title">Speeches</h1>
    <p>We don't have any published speeches at present.</p>
  <% else %>
    <%= render partial: "speeches", object: @speeches %>
  <% end %>
</div>