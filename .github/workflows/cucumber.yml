name: Run Cucumber

on: workflow_call

jobs:
  feature-test-matrix:
    name: Test features / ${{ matrix.shard_group }}-${{ matrix.ci_node_index }}
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - shard_group: non-javascript
            cucumber_tag_expression: "not @javascript"
            ci_node_total: 3
            ci_node_index: 0
          - shard_group: non-javascript
            cucumber_tag_expression: "not @javascript"
            ci_node_total: 3
            ci_node_index: 1
          - shard_group: non-javascript
            cucumber_tag_expression: "not @javascript"
            ci_node_total: 3
            ci_node_index: 2
          - shard_group: javascript
            cucumber_tag_expression: "@javascript"
            ci_node_total: 1
            ci_node_index: 0
    steps:
      - name: Setup MySQL
        id: setup-mysql
        uses: alphagov/govuk-infrastructure/.github/actions/setup-mysql@main

      - name: Setup Redis
        uses: alphagov/govuk-infrastructure/.github/actions/setup-redis@main

      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          show-progress: false

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      - name: Setup Node
        uses: alphagov/govuk-infrastructure/.github/actions/setup-node@main

      - name: Cache Playwright Chromium browser
        if: matrix.shard_group == 'javascript'
        id: playwright-cache
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-browsers-${{ runner.os }}-${{ hashFiles('yarn.lock') }}

      - name: Install Playwright Chromium browser (with deps)
        if: matrix.shard_group == 'javascript' && steps.playwright-cache.outputs.cache-hit != 'true'
        run: yarn run playwright install --with-deps chromium

      - name: Install Playwright Chromium browser deps
        if: matrix.shard_group == 'javascript' && steps.playwright-cache.outputs.cache-hit == 'true'
        run: yarn run playwright install-deps chromium

      - name: Precompile assets
        uses: alphagov/govuk-infrastructure/.github/actions/precompile-rails-assets@main

      - name: Initialize database
        env:
          RAILS_ENV: test
          TEST_DATABASE_URL: ${{ steps.setup-mysql.outputs.db-url }}
        run: bundle exec rails db:setup

      - name: Run cucumber
        env:
          RAILS_ENV: test
          TEST_DATABASE_URL: ${{ steps.setup-mysql.outputs.db-url }}
          CI_NODE_TOTAL: ${{ matrix.ci_node_total }}
          CI_NODE_INDEX: ${{ matrix.ci_node_index }}
        run: bundle exec ./bin/cucumber-ci --tags "${{ matrix.cucumber_tag_expression }}"

  run-cucumber:
    name: Run Cucumber
    needs: feature-test-matrix
    runs-on: ubuntu-latest
    steps:
      - run: echo "All feature tests have passed 🚀"
